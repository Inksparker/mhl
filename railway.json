{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "export NODE_ENV=development && cd backend && npm install --include=dev && npm run build"
  },
  "deploy": {
    "startCommand": "cd backend && node dist/migrate.js && node dist/index.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
